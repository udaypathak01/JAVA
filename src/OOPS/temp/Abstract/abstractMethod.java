package OOPS.temp.Abstract;

public abstract class abstractMethod {

    abstract void career(String name);

}
// an abstact method is a method that is declared but not implemented(no body).it only tells what should be done,
// not how it will be done.

// note => if one abstract method declared in some class,that class must be abstract.

// An abstract class is a class that cannot be instantiated (object nahi bana sakte) and is mainly used
// as a base (parent) class to provide a common structure for other classes.

